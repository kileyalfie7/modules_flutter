🚀 Các tính năng chính:

1. Token Management & Auto Refresh:

Tự động lưu/load access token và refresh token
Auto refresh token khi hết hạn
Xử lý queue requests khi đang refresh
Callback functions khi token expired

2. Caching System (giống React Query):

Memory cache cho GET requests
Cache invalidation thông minh
Stale-while-revalidate pattern
Force refresh option

3. Repository Pattern (giống Hooks):

UserRepository, AuthRepository, ProductRepository, OrderRepository
RepositoryManager để truy cập dễ dàng
Automatic cache invalidation sau mutations

4. Error Handling & Retry:

Comprehensive error handling
Auto retry với refresh token
Network error detection
User-friendly error messages

🎯 Cách sử dụng đơn giản:
dart// 1. Setup trong main.dart
ApiService.initialize(
baseUrl: 'https://api.example.com',
onRefreshTokenExpired: () {
// Chuyển về login
},
);

// 2. Sử dụng trong screen
final response = await RepositoryManager.product.getProducts(
page: 1,
forceRefresh: false, // Sử dụng cache nếu có
);

if (response.success) {
setState(() {
products = response.data!.products;
});
}

🔥 So sánh với React Query:
React QueryFlutter Module nàyuseQuery()RepositoryManager.product.getProducts()useMutation()RepositoryManager.product.createProduct()queryClient.invalidateQueries()\_apiService.invalidateCache()Auto refetchBuilt-in với forceRefreshBackground updatesCache stale-while-revalidate
📱 Ví dụ thực tế:

Login/Logout với auto token management
Product listing với pagination + cache
Search với debounce
Profile management với file upload
Order history với pull-to-refresh

Module này giúp bạn:

✅ Giảm boilerplate code
✅ Tự động xử lý tokens
✅ Cache thông minh
✅ Error handling nhất quán
✅ Easy testing và maintenance
