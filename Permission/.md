# File: pubspec.yaml

name: permission_manager_app
description: Ứng dụng quản lý quyền truy cập hoàn chỉnh

# Phiên bản ứng dụng

version: 1.0.0+1

environment:
sdk: '>=3.0.0 <4.0.0'
flutter: ">=3.0.0"

dependencies:
flutter:
sdk: flutter

# Thư viện chính để quản lý permissions

permission_handler: ^11.0.1

# UI và utility libraries

cupertino_icons: ^1.0.2

dev_dependencies:
flutter_test:
sdk: flutter
flutter_lints: ^3.0.0

# Cấu hình Flutter

flutter:
uses-material-design: true

# Assets (nếu cần)

# assets:

# - images/

# Fonts (nếu cần)

# fonts:

# - family: Schyler

# fonts:

# - asset: fonts/Schyler-Regular.ttf

  <!-- File: android/app/src/main/AndroidManifest.xml -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Khai báo các quyền cần thiết -->

    <!-- Quyền Camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera2" android:required="false" />

    <!-- Quyền Microphone -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Quyền Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Quyền background location (Android 10+) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Quyền Storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- Storage permissions cho Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Quyền Contacts -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <!-- Quyền Notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Quyền Phone (nếu cần) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Quyền SMS (nếu cần) -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- Quyền Calendar (nếu cần) -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:label="Permission Manager App"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Metadata để Flutter hoạt động -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- Intent filter cho main activity -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Metadata để Flutter tự động sinh plugin registrations -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <!-- Queries để có thể mở Settings app -->
    <queries>
        <intent>
            <action android:name="android.settings.APPLICATION_DETAILS_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.LOCATION_SOURCE_SETTINGS" />
        </intent>
    </queries>

</manifest>

<!-- File: ios/Runner/Info.plist -->

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Cấu hình app cơ bản -->
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleDisplayName</key>
    <string>Permission Manager App</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>permission_manager_app</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>$(FLUTTER_BUILD_NAME)</string>
    <key>CFBundleSignature</key>
    <string>????</string>
    <key>CFBundleVersion</key>
    <string>$(FLUTTER_BUILD_NUMBER)</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>
    <key>UIMainStoryboardFile</key>
    <string>Main</string>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    
    <!-- ========== PERMISSION USAGE DESCRIPTIONS ========== -->
    <!-- Đây là các thông báo sẽ hiển thị khi yêu cầu quyền -->
    
    <!-- Quyền Camera -->
    <key>NSCameraUsageDescription</key>
    <string>Ứng dụng cần quyền truy cập camera để chụp ảnh và quay video.</string>
    
    <!-- Quyền Microphone -->
    <key>NSMicrophoneUsageDescription</key>
    <string>Ứng dụng cần quyền truy cập microphone để ghi âm và thực hiện cuộc gọi.</string>
    
    <!-- Quyền Photo Library -->
    <key>NSPhotoLibraryUsageDescription</key>
    <string>Ứng dụng cần quyền truy cập thư viện ảnh để lưu và chọn hình ảnh.</string>
    
    <!-- Quyền Photo Library - Add Only (iOS 11+) -->
    <key>NSPhotoLibraryAddUsageDescription</key>
    <string>Ứng dụng cần quyền để lưu ảnh vào thư viện của bạn.</string>
    
    <!-- Quyền Location - When In Use -->
    <key>NSLocationWhenInUseUsageDescription</key>
    <string>Ứng dụng cần quyền truy cập vị trí khi sử dụng để cung cấp dịch vụ định vị.</string>
    
    <!-- Quyền Location - Always -->
    <key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
    <string>Ứng dụng cần quyền truy cập vị trí để cung cấp dịch vụ định vị ngay cả khi không sử dụng app.</string>
    
    <!-- Quyền Location - Always (iOS 10 và cũ hơn) -->
    <key>NSLocationAlwaysUsageDescription</key>
    <string>Ứng dụng cần quyền truy cập vị trí để cung cấp dịch vụ định vị liên tục.</string>
    
    <!-- Quyền Contacts -->
    <key>NSContactsUsageDescription</key>
    <string>Ứng dụng cần quyền truy cập danh bạ để tìm kiếm và kết nối với bạn bè.</string>
    
    <!-- Quyền Calendar -->
    <key>NSCalendarsUsageDescription</key>
    <string>Ứng dụng cần quyền truy cập lịch để tạo và quản lý sự kiện.</string>
    
    <!-- Quyền Reminders -->
    <key>NSRemindersUsageDescription</key>
    <string>Ứng dụng cần quyền truy cập nhắc nhở để tạo và quản lý công việc.</string>
    
    <!-- Quyền Speech Recognition -->
    <key>NSSpeechRecognitionUsageDescription</key>
    <string>Ứng dụng cần quyền nhận dạng giọng nói để chuyển đổi giọng nói thành văn bản.</string>
    
    <!-- Quyền Face ID / Touch ID -->
    <key>NSFaceIDUsageDescription</key>
    <string>Ứng dụng sử dụng Face ID để xác thực và bảo mật tài khoản của bạn.</string>
    
    <!-- Quyền Motion & Fitness -->
    <key>NSMotionUsageDescription</key>
    <string>Ứng dụng cần quyền truy cập cảm biến chuyển động để theo dõi hoạt động thể chất.</string>
    
    <!-- Quyền Health Kit (nếu cần) -->
    <key>NSHealthUpdateUsageDescription</key>
    <string>Ứng dụng cần quyền cập nhật dữ liệu sức khỏe.</string>
    <key>NSHealthShareUsageDescription</key>
    <string>Ứng dụng cần quyền đọc dữ liệu sức khỏe để theo dõi tình trạng sức khỏe của bạn.</string>
    
    <!-- Quyền Bluetooth -->
    <key>NSBluetoothAlwaysUsageDescription</key>
    <string>Ứng dụng cần quyền Bluetooth để kết nối với các thiết bị ngoại vi.</string>
    <key>NSBluetoothPeripheralUsageDescription</key>
    <string>Ứng dụng cần quyền Bluetooth để kết nối với các thiết bị khác.</string>
    
    <!-- Quyền Media Library -->
    <key>NSAppleMusicUsageDescription</key>
    <string>Ứng dụng cần quyền truy cập thư viện nhạc để phát nhạc.</string>
    
    <!-- Quyền Local Network (iOS 14+) -->
    <key>NSLocalNetworkUsageDescription</key>
    <string>Ứng dụng cần quyền truy cập mạng cục bộ để kết nối với các thiết bị trong cùng mạng.</string>
    
    <!-- ========== BACKGROUND MODES ========== -->
    <!-- Nếu app cần chạy background -->
    <key>UIBackgroundModes</key>
    <array>
        <string>location</string>
        <string>background-fetch</string>
        <string>background-processing</string>
    </array>
    
    <!-- ========== URL SCHEMES ========== -->
    <!-- Để có thể mở Settings app -->
    <key>LSApplicationQueriesSchemes</key>
    <array>
        <string>app-settings</string>
        <string>App-Prefs</string>
    </array>
    
    <!-- ========== ADDITIONAL CONFIGURATIONS ========== -->
    
    <!-- Cho phép HTTP requests (nếu cần) -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsArbitraryLoads</key>
        <true/>
    </dict>
    
    <!-- Status bar style -->
    <key>UIStatusBarHidden</key>
    <false/>
    <key>UIViewControllerBasedStatusBarAppearance</key>
    <false/>
    
</dict>
</plist>
